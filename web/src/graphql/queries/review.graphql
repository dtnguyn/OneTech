query Reviews($deviceId: String!) {
  reviews(deviceId: $deviceId) {
    data {
      id
      title
      content
      createdAt
      deviceId
      rating {
        reviewId
        deviceId
        overall
        battery
        software
        display
        camera
        processor
      }
    }
  }
}
